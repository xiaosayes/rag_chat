# === 核心依赖 ===
# 阿里云百炼 SDK（DashScope）
dashscope>=1.20.0

# 语音功能（bug-121）
websocket-client>=1.7.0     # 讯飞 IAT WebSocket（ASR）
static-ffmpeg>=1.5.0        # gradio 6 原生 HLS 流式音频输出所需（自带 ffmpeg/ffprobe 二进制，跨平台）
# 语音助手（audit-ASR）：silero VAD 推理。模型已内置仓库 src/assets/silero_vad.onnx
# （MIT 许可，silero-vad v6 16k 导出）——不要 pip install silero-vad（顶层依赖 torchaudio 重依赖）
onnxruntime>=1.16.0         # silero VAD ONNX 推理（src/vad.py 自持调用，纯 CPU 足够）

# 向量数据库
qdrant-client>=1.9.0

# Embedding 相关
sentence-transformers>=3.0.0  # 备用本地 embedding

# 文档处理
langchain>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# 数据处理
pandas>=2.1.0
numpy>=1.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP 客户端
httpx>=0.27.0
aiohttp>=3.9.0

# 工具
python-dotenv>=1.0.0
tqdm>=4.66.0
rich>=13.7.0           # 美化输出
loguru>=0.7.2          # 日志

# 检索增强
rank-bm25>=0.2.2       # BM25 检索
scikit-learn>=1.5.0    # 本地 rerank 备用

# 开发/测试
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ============================================
# Web UI (Gradio)
# bug-099/100：Gradio 6.x 依赖新版 Starlette（>=1.0.1），但 starlette 1.4.0 的
#   GZipResponder 新增必填 keyword-only 参数 thread_minimum_size，与 gradio 6.22 的
#   brotli_middleware 调用签名不兼容（访问页面白屏，报 missing 'thread_minimum_size'）。
# 已验证兼容组合：gradio 6.22.0 + starlette 1.3.1 + fastapi 0.141.1
# ============================================
gradio>=4.44.0
starlette>=1.0.1,<1.4
fastapi>=0.115.2,<1.0

# ============================================
# 多格式文档支持
# ============================================

# --- PDF 解析 ---
pypdf>=3.17.0

# --- Word 文档 ---
python-docx>=1.1.0

# --- PPT 解析 ---
python-pptx>=0.6.23

# --- Excel 数据源（bug-109） ---
# openpyxl>=3.1.0  # Excel (.xlsx) 表格数据作为知识库数据源（可选，缺失时仅 Excel 功能不可用）
openpyxl>=3.1.0

# --- Markdown ---
markdown>=3.5.0

# --- 图片 OCR ---
# 方案一：PaddleOCR（GPU 加速，中文效果最好）
# 安装: pip install paddlepaddle-gpu paddleocr
# 方案二：Tesseract OCR（备选）
# 安装: pip install pytesseract Pillow
#       系统还需安装 tesseract-ocr

# Pillow (图片处理)
Pillow>=10.1.0

# OpenCV (图片处理，如旋转、缩放、水印等)
opencv-python>=4.8.0

# 图片 EXIF 读取
piexif>=1.1.3

# PaddleOCR 相关（GPU 服务器上安装，可选）
# paddlepaddle-gpu>=2.6.0
# paddleocr>=2.7.0

# pytesseract 备选（可选）
# pytesseract>=0.3.10