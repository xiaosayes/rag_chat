# 数字人问答一体机 — 前后端整合与现场部署落地方案

> 版本：v1.1　日期：2026-08-12
> 适用项目：文化知识库 RAG 问答系统（当前 v1.4.0-pre，jiabohui / 数字人小虎）
> 目标环境：**Windows 竖屏一体机 + 有外网**（已确认）
> 关联文档：`README.md`、`DEPLOY_GUIDE.md`、`project-context.md`、`code_review_report_v3.md`

### 更新记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-12 | 初稿（方案 A 确认：Windows 一体机 + 有外网） |
| v1.1 | 2026-08-12 | 确认三项关键输入：① 数字人 = **3D 卡通模型 + 动作帧**，口型按文本长度驱动开合（非音素级）；② **竖屏**，UI 设计稿全部提供、前端完整复刻；③ **唤醒词"你好小虎"自动唤醒**（新增 KWS 方案 §5.5）。同步更新：五态模型、前端选型、架构图、验收标准、风险与开放问题 |

---

## 0. 方案摘要（TL;DR）

| 项 | 决策 |
|---|---|
| 总体架构 | **前后端分离**：FastAPI 后端（复用现有 RAG/ASR/TTS 内核）+ 独立前端 SPA（**按设计稿 1:1 完整复刻**，竖屏） |
| 界面形态 | 竖屏一体机，Chrome **kiosk 全屏**打开 `http://localhost:8000`，无人值守 |
| 唤醒方式 | **唤醒词"你好小虎"**：sherpa-onnx WASM 在**前端本地**常驻检测（音频不出本机），播报期间挂起防自唤醒（§5.5） |
| 数字人形象 | **3D 卡通模型**（three.js 渲染 + 动作帧状态机）；口型按**文本长度/播报时长**驱动开合循环，播报结束强制闭合，不做音素级唇同步（§5.3） |
| 部署位置 | **后端跑在一体机本机**（conda 环境，NSSM 注册为 Windows 服务），不依赖额外服务器；外网仅用于阿里云百炼 / 讯飞 API 调用 |
| Gradio 去向 | 保留为**内部调试台**（维护/验收用），生产界面走新前端 |
| 语音链路 | ASR：唤醒后浏览器录音 → WebSocket → 讯飞 IAT（复用 `src/asr.py`）；TTS：LLM 流式文本 → CosyVoice 单会话流式合成（复用 v1.4.0 泵线程/ADTS 管线）→ 前端 hls.js / Web Audio 播放 |

---

## 1. 背景与现状盘点

### 1.1 已有资产（可直接复用，不重写）

| 资产 | 位置 | 复用方式 |
|---|---|---|
| RAG 流水线（分层意图分类 + 混合检索 + 重排 + 流式 LLM） | `src/rag_pipeline.py` 等 `src/` 全部 | 后端内核原样保留 |
| ASR 讯飞听写（WebSocket 流式、VAD、热词/纠错） | `src/asr.py` + `app.py: asr_stream_chunk/stop` | 会话逻辑下沉为服务层，传输从 Gradio 回调改为自有 WebSocket |
| TTS 流式播报（单会话合成、泵线程、ADTS 持续编码、静默压缩、看门狗） | `src/tts.py`、`src/audio_bootstrap.py`、`app.py: _AdtsStreamer/_PauseCompressor` | **服务端逻辑全部保留**；播放端从"Gradio 组件 + monkeypatch"换成前端自主控制的 hls.js |
| 多项目架构（jiabohui 配置与 Prompt 人设） | `src/project.py`、`data/projects/jiabohui.json` | 原样保留 |
| 已构建知识库（171 切片，v4 向量） | `data/processed/jiabohui/` | **随部署包整体拷贝，现场不重建** |
| 505 项离线测试 | `tests/` | 重构期间作为回归护栏 |

### 1.2 现状的结构性问题

当前 `app.py`（1653 行）是 **Gradio 前后端一体**架构：界面布局、样式、交互全部由 Gradio 组件决定，只能通过 CSS/JS 注入做有限定制（v1.3.4~v1.4.0 已为 Gradio 6 兼容性付出多轮修复成本）。设计稿 UI 是定制视觉（3D 数字人、品牌视觉、定制动效、竖屏布局），**Gradio 无法 1:1 还原**，必须前后端分离。

---

## 2. 总体架构

### 2.1 方案对比（已选定）

| 维度 | 方案 A：FastAPI + 独立前端（**已选定**） | 方案 B：Gradio 深度定制 | 方案 C：FastAPI 外壳 + iframe 嵌 Gradio |
|---|---|---|---|
| 设计稿还原度 | ★★★ 任意像素级 | ★ 受组件模型限制 | ★ 同 B |
| 语音链路控制 | 完全自主（WS/HLS 直控） | 依赖 monkeypatch（脆弱，已实证） | 同 B |
| 3D 数字人接入 | three.js 自由渲染 | 不可行 | 不可行 |
| 唤醒词常驻检测 | 前端 WASM 本地运行 | 不可行 | 不可行 |
| 开发量 | 中（前端新建 + 后端加 API 层，内核零改动） | 小但不可控 | 最小 |
| 长期维护 | 清晰分层，契约明确 | 每次 Gradio 升级都是风险 | 双份复杂度 |

### 2.2 目标架构图

```
┌────────────────────────── 竖屏一体机（Windows，有外网）──────────────────────┐
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Chrome --kiosk http://localhost:8000   （前端 SPA，竖屏静态页）      │   │
│  │  ├─ 数字人渲染层：three.js 3D 卡通模型 + 动作帧状态机                 │   │
│  │  │   （口型按文本长度驱动开合，播报结束闭合）                         │   │
│  │  ├─ 唤醒词 KWS：sherpa-onnx WASM 本地常驻检测"你好小虎"              │   │
│  │  │   （音频不出本机；播报期间挂起，防自唤醒）                         │   │
│  │  ├─ 对话 UI（按设计稿 1:1：欢迎页/字幕/引导问题/五态动效）            │   │
│  │  ├─ 录音：MediaRecorder → WebSocket 上行（唤醒后才开启）              │   │
│  │  └─ 播报：hls.js / Web Audio ← 音频流下行                            │   │
│  └──────────────┬───────────────────────────────────────────────────────┘   │
│                 │ HTTP(SSE) + WebSocket   （本机回环，无网络依赖）           │
│  ┌──────────────▼───────────────────────────────────────────────────────┐   │
│  │  FastAPI 服务层（新增，薄层）  uvicorn，NSSM 注册为 Windows 服务       │   │
│  │  ├─ /api/chat/stream   问答流式（SSE）                                │   │
│  │  ├─ /ws/asr            语音转写（唤醒后建立会话）                     │   │
│  │  ├─ /ws/tts 或 /tts/*  播报音频流（复用 HLS 发布管线）                │   │
│  │  ├─ /api/projects /api/health /api/status /api/chat/reset            │   │
│  │  └─ 静态托管前端构建产物 + 3D 模型/动作帧资源                         │   │
│  └──────────────┬───────────────────────────────────────────────────────┘   │
│  ┌──────────────▼───────────────────────────────────────────────────────┐   │
│  │  业务内核（现有代码，零改动）                                          │   │
│  │  RAGPipeline · ProjectManager · asr.py · tts.py · _AdtsStreamer ...   │   │
│  │  本地存储：Qdrant(本地持久化) · BM25 · chunks.json · 缓存              │   │
│  └──────────────┬───────────────────────────────────────────────────────┘   │
└─────────────────┼────────────────────────────────────────────────────────────┘
                  │ 仅 API 流量走外网（HTTPS/WSS，流量小）
        ┌─────────▼──────────┐   ┌──────────────────┐
        │ 阿里云百炼          │   │ 讯飞开放平台      │
        │ qwen-plus / v4 向量 │   │ IAT 语音听写      │
        │ rerank / cosyvoice  │   └──────────────────┘
        └────────────────────┘

  （可选）Gradio 调试台 app.py 保留在 7860 端口，仅维护人员使用
```

### 2.3 目录结构调整（新增部分用 ★ 标记）

```
test/  （项目根）
├── app.py                    # Gradio 调试台（保留，不再承担生产界面）
├── api_server.py             # ★ FastAPI 入口：路由注册、静态托管、中间件
├── src/
│   ├── ...（现有内核，零改动）
│   └── services/             # ★ 服务层：把 app.py 中的业务逻辑下沉
│       ├── chat_service.py   # ★ 问答编排（原 respond/answer_question 去 Gradio 化）
│       ├── asr_service.py    # ★ ASR 会话管理（包装 src/asr.py）
│       └── tts_service.py    # ★ TTS 泵线程/ADTS 管线（从 app.py 平移）
├── web/                      # ★ 前端工程（Vue 3 + Vite + TS，竖屏）
│   ├── src/
│   │   ├── components/       # ★ 设计稿组件（对话区/字幕条/引导问题…）
│   │   ├── api/              # ★ API 客户端（fetch/SSE/WS 封装，含 Mock 开关）
│   │   ├── avatar/           # ★ three.js 数字人渲染 + 动作状态机 + 口型驱动
│   │   ├── kws/              # ★ sherpa-onnx WASM 唤醒词检测
│   │   └── assets/           # ★ 设计稿切图/字体；3D 模型（GLB）与动作帧
│   └── dist/                 # ★ 构建产物（由 api_server.py 静态托管）
├── data/（现有：projects/raw/processed/voice）
├── tests/（现有 505 项 + ★ 新增 API 契约测试）
└── deploy/                   # ★ 一体机部署资产
    ├── kiosk/                # ★ 开机自启脚本、kiosk 快捷方式、看门狗
    ├── nssm/                 # ★ 服务注册脚本
    └── SETUP_CHECKLIST.md    # ★ 现场部署核对单
```

---

## 3. 前后端接口契约（草案 — 联调前冻结）

> 原则：**契约先行**。本文档评审通过后，前端按契约 + Mock 开发，后端按契约实现，互不阻塞。
> 以下字段名为草案，联调 Kickoff 时冻结并写入 `docs/api-contract.md`。

### 3.1 问答（流式）— `POST /api/chat/stream`（SSE）

```jsonc
// 请求
{ "question": "推荐几个适合小户型的沙发", "project_id": "jiabohui", "session_id": "kiosk-01" }

// 响应：text/event-stream，事件序列
event: meta      data: {"query_type":"recommendation","chunks":[...],"timing":{...}}   // 检索可视化数据
event: token     data: {"text":"**1. "}        // 逐 token，多次
event: token     data: {"text":"云朵沙发"}
event: done      data: {"answer_id":"a-20260812-0001","text_length":152,"timing":{"total":1.83}}
event: error     data: {"code":"LLM_TIMEOUT","message":"..."}   // 仅出错时
```

> `done.text_length`：回答正文字符数，供前端口型开合循环做节拍依据（§5.3）。

### 3.2 语音转写 — `WebSocket /ws/asr`

| 方向 | 内容 |
|---|---|
| 上行（前端→后端） | 二进制音频帧（webm/opus 或 pcm16，握手时声明）。**会话由前端在 KWS 唤醒成功后发起**；未唤醒不建连、不上行任何音频 |
| 下行 | `{"type":"partial","text":"推荐几个"}` 增量转写（边说边出字） |
| 下行 | `{"type":"final","text":"推荐几个适合小户型的沙发"}`（VAD 静默 2s 自动结束，`ASR_VAD_EOS`） |
| 下行 | `{"type":"error","message":"..."}` |

服务端复用 `src/asr.py` 会话 + 纠错词典（`data/voice/*_asr_dict.json`）；单次会话上限 `ASR_MAX_DURATION=30s` 兜底。

### 3.3 语音播报 — `POST /api/tts/speak` → 返回流地址，前端 hls.js 播放

```jsonc
// 请求（answer_id 关联，或直接给文本）
{ "answer_id": "a-20260812-0001", "text": "（可选，直接合成）", "session_id": "kiosk-01" }
// 响应
{ "playlist_url": "/tts/stream/a-20260812-0001.m3u8", "replay_url": "/tts/replay/a-20260812-0001.wav" }
```

- 服务端：复用 v1.4.0 单会话流式合成 + `_AdtsStreamer` 持续编码 + `_PauseCompressor`（**这些已 E2E 验证，原样平移**）。
- 前端：hls.js 直连 `playlist_url`。**删除** `audio_bootstrap.py` 中的 Gradio monkeypatch 层（那是为 Gradio 组件打的补丁，自研前端用不到）；保留 TD clamp / 转码提速 / ADTS 直通等与服务端发布相关的部分。
- 播放生命周期事件（`playing` / `ended`）驱动数字人动作状态机与口型开合（§5.3）。

### 3.4 辅助接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 存活探针（看门狗用）：`{"ok":true}` |
| GET | `/api/status` | 知识库/项目/密钥配置状态（运维页用） |
| GET | `/api/projects` | 项目列表（一体机固定单项目，仅调试用） |
| POST | `/api/chat/reset` | 清空会话（超时无人操作自动复位用） |

> 唤醒词检测**无服务端接口**：KWS 完全在前端本地运行（§5.5），不消耗任何 API。

### 3.5 会话模型与待机流（一体机单用户场景）

- `session_id` 由前端生成（机器 ID 固定），对话历史服务端按 session 保留最近 4 轮（与现有 `_convert_history` 一致）。
- **主循环**：待机（KWS 监听中）→ 唤醒"你好小虎" → 应答（数字人回应"我在，请讲"）→ 聆听（ASR）→ 思考（LLM）→ 播报（TTS）→ **回到待机并恢复 KWS 监听**。
- **自动复位**：120s 无交互（未唤醒/无提问）→ 调 `/api/chat/reset` + 回欢迎页，保证下一位观众从零开始。

---

## 4. 模块职责与三方协调（前端 / 后端 / UI）

### 4.1 职责边界

| 角色 | 负责 | 不负责 |
|---|---|---|
| **UI 设计** | **竖屏设计稿全套（由用户方提供）**：全部页面、五态动效说明、切图、字体、**3D 卡通模型与动作帧资源** | 不写代码 |
| **前端** | 按设计稿 **1:1 完整复刻**；three.js 数字人渲染与口型；KWS 唤醒；录音/播放；Mock 数据先行 | 不碰 RAG/语音算法 |
| **后端** | `api_server.py` + `services/` 薄层；内核维护；部署脚本 | 不碰视觉 |

### 4.2 设计稿与资源交付清单（向 UI 侧明确提出）

1. **竖屏设计稿全套**（Figma/即时设计/蓝湖可标注链接 + 导出图）：以一体机实际分辨率为基准（竖屏常见 1080×1920，**以设计稿标注为准**）。
2. 切图资源：SVG 优先，位图给 @2x；字体文件（注意商用授权）。
3. **3D 数字人资源**：
   - 模型文件：**GLB/glTF 优先**（含骨骼/形变目标；面数与贴图尺寸需控制，见 §5.3 性能预算）；
   - **动作帧清单**：每个动作的名称、时长、是否循环（至少覆盖下方五态）；
   - 嘴部开合的实现形式：blendshape / 骨骼 / 替换贴图（三选一，建模侧标注）。
4. **五态设计**（语音交互核心，缺一不可）：
   - **待机态**（欢迎页 + 数字人 idle 循环动作；此态 KWS 常驻监听，可加"说『你好小虎』开始提问"引导文案）
   - **唤醒应答态**（被唤醒后的回应动作/音效，约 1s）
   - **聆听态**（收音中，音量波形反馈）
   - **思考态**（等待 LLM，loading 动效）
   - **播报态**（字幕随 TTS 滚动 + 数字人 talk 动作 + 口型开合）
5. 引导问题区（3~5 个预设问题按钮，观众不愿说话也能点按体验）。
6. 异常态设计：未唤醒提示 / 识别失败 / 网络异常 / 无答案 的提示样式。

### 4.3 协调工作流

```
Kickoff（冻结契约 + 确认设计稿分辨率基准 + 3D 资源清单）
   │
   ├─ 前端：Vite 工程 + Mock 数据（api/ 层内置 mock 开关）
   │        → 页面/动效/五态/three.js 数字人/KWS 全部先做
   ├─ 后端：api_server.py + services/ 平移 → 契约测试 → 自测页（最简 HTML）验证语音链路
   └─ UI：走查模板（标注间距/字号/色值/动效时长）+ 3D 动作帧验收标准
   │
联调（前端关 Mock → 接 localhost:8000）→ UI 走查（对照设计稿逐项打勾）
   │
一体机试部署（§6）→ 现场验收（§7）
```

**用 pi 并行推进**：「前端页面」「后端 API 层」「部署脚本」三个子任务契约冻结后无共享状态，可并行开发，最后统一联调。

---

## 5. 关键技术点

### 5.1 前端技术选型

- **Vue 3 + Vite + TypeScript**（团队若更熟 React 可平替，架构不变）。
- **3D 渲染**：three.js（`GLTFLoader` + `AnimationMixer`），详见 §5.3。
- **唤醒词**：sherpa-onnx WASM（本地 KWS），详见 §5.5。
- 音频播放：`hls.js`（与现有服务端 HLS 发布管线匹配）。
- 录音：`MediaRecorder`（webm/opus），`getUserMedia` 开 `echoCancellation: true`（抑制播报回串）；服务端已有 ffmpeg 转码链路（`static-ffmpeg` 自带二进制，一体机免装 ffmpeg）。
- **麦克风常驻**：待机态即持有麦克风流（仅供本地 KWS 环形缓冲，**不上传、不落盘**），唤醒后复用同一流开始上行，避免二次授权与开启延迟。
- 触屏适配：全站禁用双指缩放/长按右键/文本选中；按钮最小 48px；**竖屏布局锁定**（Windows 显示方向设为纵向，CSS 按设计稿尺寸等比缩放适配）。
- **无人值守复位**：120s 无交互 → 回欢迎页 + `/api/chat/reset`；页面心跳检测后端 `/api/health`，断连自动重试 + 友好兜底页。

### 5.2 后端改造（内核零改动，只加薄层）

1. `services/chat_service.py`：把 `app.py: respond/answer_question` 中的编排逻辑（泵线程、payload 组装）去 Gradio 化，产出 `(meta, token_iter)`。
2. `services/asr_service.py`：把 `asr_stream_chunk/stop` 的状态机从 Gradio generator 改为 WS 会话对象。
3. `services/tts_service.py`：平移 `_AdtsStreamer/_PauseCompressor/看门狗`，HLS 写入自有目录 `/tts/stream/`。
4. `api_server.py`：FastAPI 路由 + SSE/WS + 静态托管 `web/dist/`（含 3D 模型大文件的正确 MIME/缓存策略）+ `_NoCacheAssetsMiddleware` 思想保留（HTML no-cache）。
5. 回归：`pytest tests/ -q` 505 项全绿 + 新增契约测试（SSE 事件序列、WS 协议、TTS 流起播）。

### 5.3 数字人：3D 卡通模型 + 口型驱动（已确认形态）

**形态**：卡通 3D 模型 + 若干常见动作的动作帧；**口型按文本长度控制开/合，播报结束闭合，不做严格唇音同步**。

1. **资源接入**：GLB/glTF 模型 + 动作帧（建模侧导出）。前端 `GLTFLoader` 加载，`AnimationMixer` 按动作清单建**动作状态机**，与 UI 五态一一映射：

   | UI 状态 | 数字人动作 | 循环 |
   |---|---|---|
   | 待机 | idle（待机呼吸/待邀） | 循环 |
   | 唤醒应答 | greet（打招呼） | 单次 → 聆听 |
   | 聆听 | listen（倾听/点头） | 循环 |
   | 思考 | think（思考） | 循环 |
   | 播报 | talk（说话动作） | 循环 + **口型开合** |

2. **口型驱动（简化方案，按确认需求实现）**：
   - 播报开始（hls.js `playing` 事件）→ 启动嘴部**开/合两帧循环**（blendshape 权重 0↔1 或贴图切换）；
   - 节拍依据文本长度：按 `done.text_length` ÷ 有效语速（`speech_rate=1.1` 下约 5~6 字/秒）估算总播报时长，开合周期取 150~250ms 均匀循环，与字幕滚动节奏一致；
   - 播报结束（`ended` 事件）→ **立即停止循环并闭合**（权重归 0），容错：兜底定时器 = 估算时长 ×1.2，超时强制闭合（防极端情况下"合不上嘴"）；
   - 明确不做音素级同步（卡通形象无此需求）；**可选增强**：叠加 Web Audio `AnalyserNode` 音量包络调制开合幅度（成本极低、观感更自然），默认关闭，验收后视效果开启。
3. **性能预算**（一体机多为核显）：模型面数 ≤ 5 万三角面，贴图 ≤ 2048px，渲染分辨率 ≤ 屏幕物理像素；不达标时降级预案：关阴影/降渲染比例/减帧率至 30fps。
4. **动作帧验收**：每个动作起止姿势无缝衔接循环不跳变；状态切换过渡 ≤ 300ms 淡入淡出（`crossFadeTo`）。

### 5.4 性能预算（沿用已验证数据）

| 指标 | 预算 | 依据 |
|---|---|---|
| 唤醒响应（词尾→应答动作/音效） | ≤ 500ms | 本地 KWS，无网络往返 |
| 问答首字 | ≤ 2s | 现有实测 1~2s |
| TTS 首句播报 | ≤ 2s | v1.4.0 实测 1.2~1.5s |
| ASR 出字 | 边说边出（<500ms 增量） | 讯飞 IAT 实测 |
| 全程播报零停顿 | 缓冲可吸收 <20s LLM 波动 | v1.4.0 加固结论 |
| KWS 常驻开销 | CPU < 5%（WASM） | sherpa-onnx 官方量级 |

### 5.5 唤醒词方案（KWS，"你好小虎"）

**需求**：观众说"你好小虎"即自动唤醒，全程无需触屏。

1. **技术选型对比**：

   | 方案 | 说明 | 结论 |
   |---|---|---|
   | **sherpa-onnx KWS（WASM）** | 开源免费（Apache-2.0），支持自定义中文关键词（按字/拼音建模），前端 WASM 本地运行，灵敏度可调 | **推荐主用** |
   | Picovoice Porcupine（WASM） | 商用成熟，中文自定义唤醒词需 console 训练 + 商业授权（按设备计费） | 备选（若 sherpa 实测误唤醒不达标） |
   | 服务端 VAD + 讯飞 IAT 轮询 | 需持续上行音频；IAT 单会话 30s 上限、按量计费、流量浪费 | 否决（仅作降级思路记录） |
   | 讯飞离线唤醒 SDK（MSC） | 原生 Windows SDK，浏览器前端无法直接调用，需额外原生进程桥接 | 否决（复杂度不值） |

2. **运行位置**：**前端本地**（WASM 在浏览器内跑）。麦克风音频进环形缓冲仅供本地检测——**唤醒前音频不出本机、不上传、不存储**（公共场合隐私合规要点，现场可张贴提示）。

3. **唤醒主流程**：

   ```
   待机（KWS 常驻检测）
     → 命中"你好小虎"（≤500ms）
     → 唤醒应答：数字人 greet 动作 + 应答（预录音频"我在，请讲"，零延迟；不用 TTS 实时合成）
     → 进入聆听态：复用麦克风流，建立 /ws/asr 会话开始上行
     → ASR final（VAD 静默 2s）→ 走问答 → 播报
     → 播报结束 → 回待机，恢复 KWS 检测
   ```

4. **防自唤醒（关键）**：TTS 播报期间**挂起 KWS 检测**（暂停特征计算或忽略命中），播报结束恢复；`getUserMedia echoCancellation` 作辅助。否则数字人自己说话会把自己唤醒形成死循环。
5. **打断（barge-in）**：播报中被唤醒词打断 → 停播并直接聆听，列为**二期可选**（一期播报期间不响应唤醒）。
6. **阈值标定**：sherpa-onnx 关键词阈值现场可调（配置文件外露），验收按 §7 标准标定一次；环境极嘈杂时上调阈值（宁可略降唤醒率，不可误唤醒频发）。
7. **无人值守权限**：Chrome 启动加 `--use-fake-ui-for-media-stream`（自动授予麦克风权限，避免授权弹窗卡死在无人现场；专用 kiosk 设备可接受），或以组策略对 `localhost` 放行麦克风。

---

## 6. 一体机部署方案（Windows 竖屏 + 有外网）

### 6.1 部署拓扑

**全部组件跑在一体机本机**：后端本机 conda 环境运行（localhost 回环，界面不依赖现场网络质量）；外网仅承载 API 调用（每轮问答 KB 级文本 + TTS/ASR 音频流，带宽需求 < 1 Mbps）。**不引入云端服务器**，减少现场故障面。

### 6.2 一次性装机步骤（写入 `deploy/SETUP_CHECKLIST.md`，现场照单打勾）

1. **系统准备**
   - Windows 10/11 专业版；创建自动登录账号；关闭锁屏/休眠/屏幕保护（电源计划=高性能，`powercfg` 脚本固化）。
   - **显示方向设为纵向（竖屏）**；分辨率按一体机原生（与设计稿基准核对）。
   - 暂停 Windows 更新（或设活动时段 + 禁用自动重启）：`services.msc` 停 Windows Update / 组策略。
   - 关闭触屏边缘手势（设置 → 系统 → 平板电脑/触摸），禁用 Cortana/通知。
2. **运行环境**
   - 安装 Miniconda → `conda env create -f environment.yml`（或 venv + `pip install -r requirements.txt`）。
   - 无需单独装 ffmpeg（`static-ffmpeg` 自带）。
3. **应用部署**
   - 拷贝项目包（**含 `data/processed/jiabohui/` 已构建知识库**，现场不重建，避免 API 429/耗时）。
   - 写 `.env`（DASHSCOPE_API_KEY、XFYUN 三件套、TTS_VOICE 等）。
   - 拷贝 `web/dist/`（前端构建产物，含 3D 模型与动作帧资源，开发机 `npm run build` 产出）。
4. **服务注册（开机自启 + 崩溃自愈）**
   - 用 **NSSM** 把后端注册为 Windows 服务：`nssm install DigitalHumanAPI "<conda_python>" "E:\app\api_server.py --port 8000"`，失败自动重启（NSSM 自带 AppRestart）。
   - 启动文件夹放 **kiosk 快捷方式**：
     ```
     chrome.exe --kiosk http://localhost:8000 ^
       --autoplay-policy=no-user-gesture-required ^   ← TTS 自动播报必需
       --use-fake-ui-for-media-stream ^               ← 自动授予麦克风权限（KWS/ASR 必需）
       --disable-pinch --overscroll-history-navigation=0 ^
       --disable-features=Translate,TouchTextEditingRedesign ^
       --no-first-run --disable-session-crashed-bubble --start-fullscreen
     ```
   - 看门狗脚本（计划任务每 1 分钟）：探测 `http://localhost:8000/api/health`，连续失败则 `nssm restart`。
5. **网络**
   - 出站白名单（如现场有网管）：`*.aliyuncs.com`（百炼/TTS）、`*.xfyun.cn`（ASR）。
   - 建议配 4G USB 网卡做备用链路；断网时前端检测 API 失败 → 播兜底话术"网络连接中，请稍后再试"。
6. **验收自测**（§7 清单，含唤醒率现场标定）

### 6.3 现场运维

- **更新**：git tag 打发布包 → U 盘拷贝（或现场有网 `git pull`）→ 运行 `deploy/kiosk/update.bat`（停服务→覆盖→起服务→开浏览器自检）。
- **知识库更新**：开发机重建 → 仅拷贝 `data/processed/jiabohui/` + `data/voice/` → 重启服务。
- **日志**：loguru 文件日志（已轮转 30 天）+ TTS 停顿遥测（`/__tts_stall`，v1.4.0 已建）；现场问题先取 `logs/`。
- **每日**：计划任务凌晨重启一次后端服务（释放内存/防长尾泄漏），开馆前自动就绪。

---

## 7. 验收标准

| # | 项 | 标准 |
|---|---|---|
| 1 | 视觉还原 | UI 走查单逐项通过（对照竖屏设计稿，间距/字号/色值/动效） |
| 2 | 五态流转 | 待机→唤醒应答→聆听→思考→播报→待机，数字人动作与 UI 状态一一对应、不错乱 |
| 3 | **唤醒** | 1 米正常音量说"你好小虎"20 次，唤醒成功率 ≥95%，响应 ≤500ms；环境噪音 1 小时误唤醒 ≤2 次；**播报期间 0 次自唤醒** |
| 4 | 语音全链路 | 唤醒→边说边出字→静默 2s 自动结束→回答流式出字→自动播报，首句 ≤2s，全程零停顿 |
| 5 | 数字人 | talk 动作期间口型按文本节奏开合，**播报结束立即闭合**；动作切换无跳变；核显下渲染流畅（≥30fps） |
| 6 | 无人值守 | 断电重启 → 3 分钟内自动进入 kiosk 欢迎页（含麦克风权限自动就绪），全程零人工操作 |
| 7 | 稳定性 | 连续运行 8h，100 轮"唤醒→问答→播报"无卡死；内存无持续增长 |
| 8 | 断网兜底 | 拔网线 → 友好提示；恢复后自动可用（KWS 本地不受影响，问答/API 恢复即正常） |
| 9 | 复位 | 120s 无操作自动回欢迎页并恢复 KWS 监听，下一位观众对话历史为零 |

---

## 8. 里程碑（建议）

| 里程碑 | 内容 | 出口 |
|---|---|---|
| M0 契约冻结 | 本文档评审通过；设计稿与 3D 资源到位（§9.1 清零） | `docs/api-contract.md` 冻结 |
| M1 后端 API 层 | services/ 平移 + 契约测试 + 自测页验证语音链路 | 505 旧测试 + 新契约测试全绿 |
| M2 前端页面 | Mock 数据下：设计稿 1:1 复刻 + 五态 + three.js 数字人（含口型）+ KWS 唤醒 | UI 走查通过（Mock 版）；本地唤醒自测 |
| M3 联调 | 前端接真后端；唤醒→问答→播报全链路 E2E | §7 验收 2~5 通过 |
| M4 试部署 | 一体机装机 + 自启 + 看门狗 + 唤醒阈值现场标定 | §7 验收 6~9 通过 |
| M5 现场 | 网络/音量/收音/噪音调试，试运行 1 天 | 现场签收 |

---

## 9. 风险与开放问题

### 9.1 开放问题（M0 前必须清零）

1. **UI 设计稿**：交付时间？设计工具（Figma/即时设计/蓝湖）？**竖屏基准分辨率**（1080×1920 还是其他）？是否已含五态与异常态？
2. **3D 资源**：模型格式是否可导出 GLB/glTF？动作帧清单（名称/时长/循环）何时提供？嘴部开合用 blendshape / 骨骼 / 替换贴图哪种？
3. **现场网络**：有线还是 Wi-Fi？是否有 802.1X/门户认证（有则需提前协调 MAC 白名单）？
4. **收音硬件**：一体机自带麦克风还是外接阵列麦？现场环境噪音水平（决定 KWS 阈值与是否加定向麦）？
5. **唤醒应答音**：默认用预录音频（"我在，请讲"，零延迟）——是否需 UI/运营提供音源？还是用 TTS 音色预生成一条保持一致？

### 9.2 风险登记

| 风险 | 影响 | 缓解 |
|---|---|---|
| 环境噪音致**误唤醒**或漏唤醒 | 体验差/自说自话 | KWS 阈值现场标定（§5.5-6）；播报期挂起 KWS 防自唤醒；外接定向/阵列麦预案；二期可加双词唤醒 |
| 播报回串进麦克风干扰 ASR | 识别错乱 | `echoCancellation` + 播报期挂起 KWS；麦克风与扬声器物理隔离建议 |
| 现场网络波动致 API 超时 | 问答/语音中断 | 现有重试 + 断网兜底话术；4G 备用链路；TTS 重播走本地文件 |
| 百炼 429 限流（已发生过） | 高峰期回答变慢 | 控制台提配额；现有退避重试；LLM 缓存吸收重复问题 |
| **3D 模型性能不足**（核显） | 卡顿/掉帧 | §5.3 性能预算与降级预案；M2 阶段真机实测提前暴露 |
| Windows 更新自动重启 | 营业中断 | 组策略禁用 + 看门狗 + 凌晨计划重启 |
| 前端长时间运行内存泄漏（含 three.js 资源） | 8h 后卡顿 | kiosk 页面每日定时 `location.reload()`；WebGL 资源释放检查；验收 #7 卡住 |
| 持续开麦的权限/隐私合规 | 无法收音/投诉 | `--use-fake-ui-for-media-stream` + 组策略；唤醒前音频本地处理不上传，现场提示语 |

---

## 10. 下一步

1. 评审本文档（v1.1）→ 清零 §9.1 开放问题（重点是**设计稿交付**与**3D 资源清单**）。
2. 冻结 `docs/api-contract.md`（从 §3 草案细化）。
3. 进入实施：按 writing-plans 产出 M1~M4 实施计划，三个子任务（前端/后端 API/部署脚本）可并行开工。
